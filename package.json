{
  "name": "render-proxy",
  "version": "1.0.0",
  "description": "Render-compatible remote browser proxy (puppeteer-core + sparticuz chromium)",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'no build step'",
    "postinstall": "echo 'postinstall (no chromium download here)'"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "ws": "^8.13.0",
    "puppeteer-core": "^22.6.0",
    "@sparticuz/chromium": "^123.0.1"
  }
}
